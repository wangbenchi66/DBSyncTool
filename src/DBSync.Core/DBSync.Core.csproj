<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- 瘦身：不再使用 Easy.SqlSugar.Core 元包（连带 Easy.Common.Core→ASP.NET Core 串扰），改用原生 sqlSugarCore；
         连接串补全（CheckTrustServerCertificate/CheckEncrypt）已内联到 Extensions/ConnectionStringExtensions.cs -->
    <PackageReference Include="sqlSugarCore" Version="5.1.4.214" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.11" />
  </ItemGroup>

</Project>
