# 12: 桌面 - SQL 脚本导出 + 差异报告

**Status:** resolved

**Blocked by:** 08 - SQL Server INSERT 脚本 + 事务封装，11 - 桌面 - 加载快照 + 差异预览

**What to build:** 实现差异预览后的最终步骤：生成 `Upgrade.sql` 并导出，同时可导出供团队审阅的差异报告。

- [x] 差异预览确认后，点击"生成脚本"调用 `GenerateUpgradeScript`，展示生成进度
- [x] 保存 `Upgrade.sql` 文件选择对话框，默认文件名含时间戳
- [x] 生成完成后展示摘要：DDL 语句数量、INSERT 语句数量、总行数估计、文件大小
- [x] 差异报告导出：支持 HTML 和 Markdown 两种格式
- [x] 历史记录：将本次生成的脚本路径写入历史，并在主界面“历史记录”页面展示
- [x] 主界面历史记录页：展示最近使用的连接和快照路径，点击可快速重新打开

## 答案

已完成第 12 项的最小可用闭环：

- 新增 `Upgrade.sql` 导出入口
- 新增 Markdown / HTML 差异报告导出
- 写入最近生成的脚本、报告和快照历史
- 主界面增加“历史记录”页
